# Vue Modals Sandbox

Sample project to try the [@noeldemartin/vue-modals](https://github.com/NoelDeMartin/vue-modals) library.

Start the project with the following commands:

```sh
pnpm install
pnpm dev
```
